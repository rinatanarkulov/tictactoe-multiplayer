apiVersion: apps/v1
kind: Deployment
metadata:
  name: tictactoe
  namespace: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tictactoe
  template:
    metadata:
      labels:
        app: tictactoe
    spec:
      containers:
      - name: tictactoe
        image: tictactoe:staging
        imagePullPolicy: Never
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: tictactoe-service
  namespace: staging
spec:
  type: NodePort
  selector:
    app: tictactoe
  ports:
  - name: web
    port: 5000
    targetPort: 5000
    nodePort: 30502
